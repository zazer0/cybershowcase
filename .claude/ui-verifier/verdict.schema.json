{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["pass", "failures", "summary"],
  "properties": {
    "pass": {
      "type": "boolean",
      "description": "true if ALL acceptance criteria are met in the screenshots"
    },
    "summary": {
      "type": "string",
      "description": "One-sentence overall assessment"
    },
    "failures": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["criterion", "explanation", "viewport"],
        "properties": {
          "criterion": {
            "type": "string",
            "description": "The acceptance criterion that failed, verbatim from the contract"
          },
          "explanation": {
            "type": "string",
            "description": "What is wrong in the screenshot and what the correct state should be"
          },
          "viewport": {
            "type": "string",
            "description": "Which viewport the failure was observed in, if viewport-specific"
          }
        },
        "additionalProperties": false
      },
      "description": "Empty array when pass is true"
    }
  },
  "additionalProperties": false
}
